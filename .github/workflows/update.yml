name: Update weather

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Generate dashboard (EN)
        run: python generate.py
      - name: Generate dashboard (PL)
        run: python generate_pl.py
      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html pl/index.html
          git diff --cached --quiet || git commit -m "Update weather $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
